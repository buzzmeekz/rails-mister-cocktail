<div class="container">
  <h1>Ingredients list</h1>
<ul>
  <% @ingredients.each do |i| %>
    <li>
      <%= link_to i.name, ingredient_path(id: i.id) %> 
      (<%= i.cocktails.count %>)
    </li>
  <% end %>
</ul>

</div>

